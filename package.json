{
  "name": "ah-memory-fs-indexed-db",
  "version": "1.0.1",
  "files": [
    "dist"
  ],
  "main": "dist/index.js",
  "module": "dist/index.js",
  "scripts": {
    "ci": "prettier --check src && tsc -p tsconfig.json --noEmit",
    "build": "tsc -p tsconfig.json --outDir dist",
    
    "release": "npm run ci && npm run build && release-it"
  },
  "author": "concefly@foxmail.com",
  "license": "MIT",
  "devDependencies": {
    "@types/lodash": "^4.14.192",
    "@types/path-browserify": "^1.0.0",
    "typescript": "^5.0.3",
    "ah-memory-fs": "^1.0.18",
    "prettier": "^3.0.2",
    "release-it": "^16.1.5"
  },
  "sideEffects": false,
  "dependencies": {
    "ah-logger": "^3.2.7"
  },
  "peerDependencies": {
    "ah-memory-fs": "^1.0.18"
  }
}
